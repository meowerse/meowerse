# Brand v2 — decision log

Every owner request and decision for the meowerse redesign onto the alxnko.dev design system, in
order, with the owner's words kept verbatim. The spec
(`docs/superpowers/specs/2026-09-24-brand-v2-design.md`) and the plans must agree with this log;
later rows override earlier ones. Status is updated as work lands.

| # | Owner said (verbatim or close) | Decision | Status |
|---|---|---|---|
| B1 | "i also like this alxnko.dev design, let's change our meowerse design lib and all meowerse services design, auth, meowsenger and everything else to have this design and this design system, let's significantly improve and update everything, make sure to use the best things for everything, maybe some small 3d things of reasonable etc., … make everything use our branding design, heavily use superpowers, /ui-ux-pro-max /ui-styling /design and all other skills … fully verified and correct, fully optimized and fast, fully polished and clear and fully protected and secured" | Redesign the meowerse design library and every meowerse frontend onto the alxnko.dev design system; significantly improve UX, performance, security and polish. Architectural work: spec → plan → one PR per sub-project. Skills used throughout: brainstorming, writing-plans, ui-ux-pro-max, ui-styling, design, requesting-code-review, verification-before-completion, shipping-to-merge. | Spec in progress |
| B2 | Scope question → "meowerse monorepo only" | In scope: `packages/ui` (+ `packages/brand`), `apps/web` (meow.alxnko.dev), `apps/auth-web` (auth.alxnko.dev), `apps/meowsenger-web` (meowsenger.alxnko.dev), retiring the obsolete `apps/alxnko-dev`. Out of scope: NextMeowsenger, LibMeowsenger, moonmeow, sunmeow, and the separate `alxnko.dev` repo (the source of the design, not a target). | Decided |
| B3 | Fidelity question → "Same system, app-shaped" | alxnko.dev tokens, graphite + one green, JetBrains Mono everywhere, a VT323 wordmark, lowercase tty voice and prompt/cursor details — but normal app UX (real forms, buttons, chat bubbles), usable by non-technical people. | Decided |
| B4 | 3D question → "Tiny 3D cat mascot, landing + sign-in" | The low-poly green cat from the alxnko.dev desk as a tiny lazy-loaded interactive model (head follows cursor/finger) in the meow.alxnko.dev hero and beside auth sign-in; static fallback; reduced-motion aware; no 3D in meowsenger. | Decided |
| B5 | Approach question → "Evolve @meowerse/ui to v2" | Rewrite tokens/fonts/styles in place and restyle the existing components keeping their APIs; add the new brand components; alxnko.dev keeps its own copy of `tokens.json` with a drift check. | Decided |
| B6 | Design presented (foundations, components, Cat3D with a custom tiny WebGL2 renderer, per-app direction, quality bar, order) → approved with: "make sure to save it and not lose and forget anything, make sure all decisions are saved and correct so we can later verify it against the specs and other docs" | The design is the spec; this log records every decision; both are committed and kept current. | Done (this file) |
| B7 | "they might have some already existing UI/UX issues make sure to investigate, find and resolve and fix them, we need to significantly improve everything" | A read-only audit of every app (live and local, phone and desktop, light and dark) before implementation; each finding gets an ID and is fixed in its sub-project; the audits are committed under `docs/superpowers/audits/`. | Audits running |
| B8 | "we can also slightly style our apps to terminal if it's good and reasonable, like maybe change input in chat to terminal prompt etc., investigate is it good for UX/UI and other things and clear and easy to understand and use" | Terminal styling is applied per element only where it keeps or improves usability, judged against ui-ux-pro-max guidelines: e.g. the chat composer as a `›` prompt line, but still a normal multiline input with a visible send button, placeholder text and IME/mobile support. Each such choice and its rationale is recorded in the spec (§7). | Decided (principle) |
| B9 | "has no blocking background logic, like we had invisible turnstile that sometimes does works and button is enabled and when click it says verification not finished, we fixed it, investigate and fix other blocking or similar issues" | The audits specifically hunt for UI that looks ready while background work isn't (captchas, tokens, session checks, uploads, websocket connects, lazy chunks, disabled-but-looks-enabled states, silent failures). Rule for all apps: controls reflect real readiness (disabled with a visible reason, or a working queue); every async wait has a visible state, a timeout and a recovery path. | Decided (principle) |
| B10 | "but also make sure this terminal style and app style components will look good together and correctly" | Terminal-styled and app-styled elements are one family, not two: they share the same tokens (surfaces, one green, radii 2/4/8, borders, focus ring, spacing, type scale), the same 44 px targets and states. Mixing rules and a side-by-side gallery page (every tty element next to the app components it sits with, phone/desktop × light/dark) are part of the design system; screenshot tests guard them, and each app page is reviewed for cohesion before merge. Spec §7.1. | Decided |
| B11 | "on meow.alxnko.dev let's also have pages dedicated to this projects like what and how works" | meow.alxnko.dev gets one page per project: what it is, how it works (plain-language overview first, then a short technical "how it works" with a diagram), its status and a link to open it. Public facts only; no secrets, internal hostnames or infra details beyond what is already public. Which projects get pages: see B13. Spec §5.1. | Decided |
| B12 | "also some page dedicated to our UI with maybe docs etc and templates of components usage and this components on the page with variables and customization possiblities and all possibilities they have etc., like some docs about all things that alxnko.dev has on it" | The dev-only gallery (§3) becomes public design-system docs at meow.alxnko.dev/ui: foundations (every token with its value in both themes, type, spacing, radii, motion, voice), every component rendered live with every variant and state, its props/CSS variables table, copyable usage snippets, and a live customisation panel (theme, props, token overrides); the tty/app "together" page (B10); Cat3D. Docs are generated from the component source and token file so they cannot drift. Covers everything the shared design system has, i.e. what alxnko.dev is built from. Spec §5.2. | Decided |
| B12a | "rn meow.alxnko.dev is kinda just plain html, it's design will be also significantly improved?" | Yes: meow.alxnko.dev is redesigned fully (hero with wordmark + Cat3D, live services list, project pages, UI docs, shared header/footer/nav, both themes, phone-first), not just re-tokened. | Decided |
| B13 | Projects question → "meowerse services, moonmeow + sunmeow, alxnko.dev" | Project pages (B11): auth (meowerse account), meowsenger, the UI library, moonmeow (phone streaming client), sunmeow (desktop host), alxnko.dev (the 3D desk site). Not: NextMeowsenger, LibMeowsenger. moonmeow/sunmeow pages carry no Tailscale, network, IP or pairing details; the alxnko.dev page never shows the real name or company. | Decided |
| B14 | (from audit M-14, applying B3's voice) | The lowercase tty voice applies only to interface copy we write (labels, headings, buttons, microcopy), authored in lowercase — never via a global `text-transform`. User content (messages, names, quotes, search snippets, codes, emails) is always shown exactly as typed. Guarded by a test. | Decided |
| B15 | (from audit L-01) | `infra/cloudflare/deploy-alxnko-dev.sh` would overwrite the live alxnko.dev (same Pages project). Retiring `apps/alxnko-dev` moves to the FIRST PR (sub-project 1), not the last, with the audit checklist (workspace, bun.lock, deploy script, `packages/brand/scripts/build.sh:88` first). Old legacy Pages deployments are left alone (owner's call). | Decided |
| B16 | (from audit W-01/W-02) | The meow.alxnko.dev "add" demo that needs a write token in the page is removed, not fixed: no write credential is ever shipped to a browser. The redesigned page is read-only (services, projects, docs). | Decided |
| B17 | (from audit mapping gaps G-1..G-12) | The tokens are extended in `@meowerse/ui` and upstreamed to alxnko.dev `tokens.json` with the drift check: `ok`, `info`, `onDanger`, `dangerTint`, `surfaceRaised`, `lineInput` (≥3:1), `scrim`, `leading`, `controlHeight` (44), `disabledOpacity`, `z.toast`. `StatusLine` uses theme tokens, not ANSI (ANSI fails light contrast). Default theme flips to dark-following-system. | Decided |
| B18 | (from audits A-03..A-08, U-01/U-02, M-05..M-09) | One shared request layer in `packages/ui` (or `ts-shared`): every fetch has a timeout, checks `res.ok`, and returns explicit `loading / ok / empty / error(kind) / signed-out` states; "signed out" only on a real 401, never on network/5xx errors. Success toasts only after a 2xx. All three apps migrate to it. | Decided |
| B19 | (from audit A-01, B9) | Turnstile readiness is shown: submit waits on a visible `StatusLine` ("checking you're human…" → ok / failed + retry); script-blocked and widget-error states are shown with a retry, never a silent enabled button. | Decided |
| B20 | (from audit A-26) | The consent screen shows the redirect host and the client id next to the app name. | Decided |
| B21 | (from audit A-18) | Username case: open question for the owner (data change). Proposed: usernames unique and matched case-insensitively, displayed as stored, `autocapitalize="none"`; a migration check lists any existing case-colliding pairs before enforcement. | Superseded by B22 |
| B22 | Usernames → "Case-insensitive" | Usernames are unique and matched case-insensitively, displayed exactly as registered, inputs use `autocapitalize="none" autocorrect="off" spellcheck="false"`. A migration first reports any existing case-colliding pairs; enforcement ships only after they are resolved (owner decides per pair). Fixed in sub-project 3 (auth), with the DB migration in `workers/auth`. | Decided |
| B23 | Analytics → "Turn it off" | Cloudflare Web Analytics / injected scripts are disabled for the meowerse zones' sites; the privacy page's "no analytics" stays true; CSP stays strict. Verified live after the change. | Decided |
| B24 | eu.org → "meow.alxnko.dev" | `meow.alxnko.eu.org` redirects to `https://meow.alxnko.dev/` (Terraform redirect rule). | Decided |
| B25 | "approved" (written spec review) | Spec `2026-09-24-brand-v2-design.md` at B1–B24 is approved; next is the implementation plan for sub-project 1. | Done |
| B26 | (PageSpeed review of alxnko.dev) "let's also add all this improvements, if some of them are applicable for other meowerse services we can also apply them here, make sure everything is fully verified and correct, fully optimized and fast, fully polished and clear, fully secured and protected but also looks the best possible" | The alxnko.dev PageSpeed fixes (measured by a profiling pass) ship there first; every one that applies to meowerse becomes a requirement for sub-projects 2–4: critical CSS inlined and hashed in the CSP, the primary font preloaded, no long main-thread tasks at start-up (split work, yield), 44 px touch targets with spacing, no Cloudflare-injected scripts (Email Obfuscation + Web Analytics off), Trusted Types where no raw-HTML sinks remain, Lighthouse ≥ 95 mobile and desktop measured in lab conditions without a GPU. | Decided |
