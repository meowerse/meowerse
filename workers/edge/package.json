{
  "name": "@meowerse/edge-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "wrangler deploy --dry-run --outdir /tmp/wrangler-dryrun",
    "secret:path": "wrangler secret put PATH_SECRET"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
