---
import "@meowerse/ui/tokens.css";
import "../styles/app.css";
import { THEME_INIT_SCRIPT, Footer } from "@meowerse/ui";
// `footer` defaults on for content pages; the full-height chat shell (/app) and
// the centered join preview (/join) pass footer={false}.
interface Props { title: string; bodyClass?: string; footer?: boolean; header?: boolean }
const { title, bodyClass, footer = true, header = false } = Astro.props;
const footerLinks = [
  { label: "about", href: "/about" },
  { label: "privacy", href: "/privacy" },
  { label: "terms", href: "/terms" },
];
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover, interactive-widget=resizes-content" />
    <title>{title}</title>
    <!-- Icons: generated by packages/brand (bash packages/brand/scripts/build.sh).
         sizes="32x32" on the .ico is what makes browsers that understand SVG skip it. -->
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <script is:inline set:html={THEME_INIT_SCRIPT}></script>
  </head>
  <body class={bodyClass}>
    {header && (
      <header class="mw-row" style="padding: var(--space-3) var(--space-gutter); align-items: center;">
        <a href="/" class="mono" style="font-family: var(--font-display); font-size: var(--text-lg); text-decoration: none;">
          ‹ meowsenger
        </a>
      </header>
    )}
    <main class="mw-container">
      <slot />
    </main>
    {footer && <Footer links={footerLinks} legal="meowsenger — a personal project by alxnko" />}
  </body>
</html>
